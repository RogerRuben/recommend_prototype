# Python 3.8 / Win64 exact-price inference environment.
# A formal bundle's manifest remains authoritative: if it references another
# library/version, prepare that dependency as well and validate on the target image.
numpy==1.23.5
scipy==1.10.1
joblib==1.1.1
threadpoolctl==3.1.0
scikit-learn==1.2.1
pandas==1.5.3
# The application serializes with standard pickle and never calls joblib.load,
# but joblib remains an installation dependency of scikit-learn itself.
# The current formal bundle declares only sklearn-backed estimators; XGBoost
# is intentionally not part of the production inference runtime.
