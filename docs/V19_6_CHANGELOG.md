# V19.6更新说明

1. 修复管理编辑保存按钮未绑定表单的问题。
2. 停用标签下的规则可以正常保存；标签存在与启用状态分离校验。
3. 数据管理增加直接启停、归档、依赖检查、受控永久删除、自动备份和审计。
4. DataMaster增加填写说明、字典下拉、动态引用下拉和布尔填写示例。
5. 价格与效能拆分为独立HTTP服务，包含单方案、批量、Schema、健康检查、OpenAPI和简易前端。
6. 价格正式模式直接保存/加载原拟合模型、Scaler、字段顺序和集成规则，不依赖joblib。
7. 效能正式模式直接运行原源码、Workbook和可选State，不强制转换成pickle。
8. 推荐系统通过完整方案JSON并行调用两个服务，并保留本地回退选项。
9. 增加Win7/Python3.8启动脚本、离线效能依赖和服务健康检查。
