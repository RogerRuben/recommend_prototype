# V18变更清单

1. 示例成品由锁类设备更换为高可靠伺服电动缸。
2. 模型契约升级为2.0，允许价格与效能属性集合不同。
3. 效能属性固定为完整可编辑成品属性，缺失即拒绝。
4. 价格模型独立声明原始字段、特征顺序和缺失策略。
5. 新增model_input_bindings数据库维护表。
6. 推荐数值默认显示三位小数。
7. 修复专家保存方案详情入口和重算流程。
8. 恢复CSV/XLSX宽表模板、预览、校验和导入。
9. 增加现场Windows 7与远端服务器两套启动配置。
10. 推荐卡片区分已匹配标签和缺失标签。
